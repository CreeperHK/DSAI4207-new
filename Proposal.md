# 課程專案提案書：多少參數才夠？以 LoRA 微調 Qwen3.5 學習數學推理

**課程**：DSAI4207
**日期**：2026 年 9 月

---

## 一、我的問題是什麼（What is your question）

**核心問題：在有限的硬體資源下，一個語言模型要改變多少、又要用多高的精度去改變，
才能真正學會一個新任務？**

具體地說，本專案要回答以下五個可測量的子問題：

1. **改變的「廣度」**：只微調注意力層、只微調 FFN 層、只微調完整注意力層，
   或是全部微調——哪一種能用最少的參數改動達到最好的數學推理表現？
2. **改變的「容量」**：LoRA 的 rank 從 4 增加到 128，效能如何變化？
   是否存在明顯的報酬遞減點？
3. **改變的「資料量」**：用 1,000 筆、5,000 筆、20,000 筆到全量資料訓練，
   正確率如何變化？「學會格式」與「學會數學」的資料量需求是否不同？
4. **★ 改變的「訓練精度代價」**：用 **QLoRA（4-bit）** 訓練，
   相對於 **bf16 LoRA**，在**固定 16 GB VRAM 的硬體條件下**，
   究竟損失多少準確率、換到多少記憶體空間與多長的上下文？
5. **改變的「部署精度代價」**：把訓練好的模型量化成 Q8 與 Q4 之後，
   微調所學到的能力還剩多少？

**研究假設**：我預期模型會在**極少量的參數改動與極少量的資料**下，
就先學會「輸出格式」（產生 `<think>` 思考區塊並以 `\boxed{}` 收尾），
但**數學正確率**需要顯著更多的參數容量與資料量才會改善。
若此假設成立，那意味著許多「微調讓模型變聰明」的報告，
實際上只證明了模型學會了格式。

**第二個假設（關於量化）**：我預期 QLoRA 在 Qwen3.5 上的品質損失
會**大於一般 Transformer 模型**，因為此模型的線性注意力層
在原始設計上需要 `float32` 精度（`mamba_ssm_dtype`）。
驗證或推翻這個假設，本身就是有價值的發現。

---

## 二、為什麼這值得做（Why is this interesting）

### 2.1 實務上的意義

微調一個 LLM 的成本，主要取決於三個選擇：**改多少參數、用多少資料、
用多高的精度部署**。這三個選擇在實務上都直接對應到金錢與時間：

- 若只微調最後四分之一的層就能達到全層微調九成的效果，訓練成本可以大幅下降。
- 若 5,000 筆資料就夠，那就不需要花錢標註或取得 50 萬筆資料。
- 若量化到 Q4 幾乎不掉分，那就能用一張消費級顯卡服務這個模型。

本專案把這三個問題放在同一個受控實驗框架下測量，產出的結論對任何想
「用有限資源微調模型」的人都有直接參考價值。

### 2.2 科學上的意義

「模型需要改變多少才能學新東西」是參數高效微調（PEFT）領域的核心問題。
既有文獻（如 LoRA 原論文）多半在較大的模型與通用任務上驗證，
**在 2B–4B 這種「消費級可訓練」的規模上，以數學推理這種
「高難度、有客觀正確答案」的任務來檢驗的文獻相對稀少。**

此外，本專案挑選的基礎模型 Qwen3.5 具有一個特殊之處：
它是**混合架構**（每 4 層中有 3 層是 Gated DeltaNet 線性注意力、
1 層是完整注意力），且**內建視覺編碼器**與 MTP 預測層。
這讓「微調哪些層」這個問題變得非平凡——線性注意力層與完整注意力層
是否需要不同程度的調整，是一個尚未被充分研究的問題。

### 2.3 為什麼選「數學推理」

數學是理想的測試任務，因為：

- **有客觀正確答案**，不需要人工評分，可完全自動化。
- **有現成的公開資料集**，且已包含推理痕跡（thinking traces），不需要自行建構。
- **對量化誤差敏感**，因此能放大「精度」這個變因的影響，讓量化實驗有訊號。
- **容易檢測「只學會格式」的假進步**：可以分別測量格式合規率與正確率。

---

## 三、我要建構或測試什麼（What will you build or test）

### 3.1 基礎模型

| 模型 | 參數量 | 語言層數 | 備註 |
| --- | --- | --- | --- |
| `Qwen/Qwen3.5-2B-Base` | 約 2.27 B | 24 | 混合架構（18 層線性 + 6 層完整注意力） |
| `Qwen/Qwen3.5-4B-Base` | 約 4.66 B | 32 | 混合架構（24 層線性 + 8 層完整注意力） |

兩者皆為 Apache-2.0 授權、公開可下載。選擇 **Base**（而非指令微調版本）是刻意的：
因為 Base 模型未經指令微調，最能乾淨地呈現「微調前 vs 微調後」的差異。
（註：檢查點另外各含 1 個 MTP 區塊，權重檔因此實際有 25 / 33 塊，
這與後續 GGUF 轉換的已知問題有關，已於計畫書第 9 章說明。）

### 3.2 資料集

使用公開的數學推理資料集，**不自行建構資料**。主要使用
**OpenR1-Math-220k**（`open-r1/OpenR1-Math-220k`，`default` config 共 93,733 題；
`all` config 共 225,129 題）。每題附有 2–4 條由 DeepSeek-R1 產生的完整
推理痕跡，並提供**逐條的官方答案驗證結果**（`correctness_math_verify`），
因此可以只保留「答案確實正確」的樣本，提升訓練訊號品質。
另以 **Bespoke-Stratos-17k** 做為小規模快速驗證。

選擇標準是資料**必須已包含思考痕跡**，且推理結尾有可自動抽取的
`\boxed{...}` 最終答案，以便自動評分。

訓練格式為：

```
<|im_start|>user
{題目}<|im_end|>
<|im_start|>assistant
<think>
{推理過程}
</think>

The final answer is \boxed{...}.<|im_end|>
```

> **注意**：Qwen3.5 的思考標籤是 `<think>` / `</think>`，
> **不是** Qwen3 時代的 ` thinking`。這兩個是不同的特殊 token
> （id 248068 與 248069），若沿用舊教學的程式碼會完全抓不到思考內容。

### 3.3 要測試的變因（實驗矩陣）

所有實驗都在單張 16 GB VRAM 的消費級顯卡上執行。
**因為 16 GB 無法進行全參數微調，本專案的方法論核心是
「用不同的參數高效微調方法，在固定硬體上換取最大能力」。**

| 實驗軸 | 變因 | 水準 |
| --- | --- | --- |
| **A. 模型規模** | 基礎模型 | 2B、4B |
| **B. LoRA 容量** | rank | 4、16、64、128 |
| **C. 資料量** | 訓練樣本數 | 1k、5k、20k、全量 |
| **D. 改動範圍** | 微調目標層 | 僅注意力、僅 FFN、全部、僅線性注意力層 |
| **★ E. 微調方法** | **訓練方法與精度** | **bf16 LoRA（主力）、DoRA、QLoRA（4-bit，含修正版與崩潰重現）** |
| **F. 部署精度** | GGUF 量化格式 | bf16、F16、Q8_0、Q4_K_M |

**E 軸是本專案的核心**，因為它直接對應課程的「Efficient Fine-Tuning」主題。
每一組都會測量三個數字：**VRAM 峰值、可訓練參數比例、準確率**，
最後畫成一張「效率前緣」圖（橫軸 VRAM、縱軸準確率）。

**E 軸已有一個具體的預期發現**：標準 QLoRA 在此架構上會崩潰
（成因、issue 與重現條件皆已查證）。本專案會以實驗記錄這個結果，
並測試「修正後能否運作、代價多少」，讓這個結論有第一手證據。

每組實驗都與**未微調的基礎模型**比較，且基礎模型會分別以
thinking 開啟、thinking 關閉、few-shot 三種方式評估（詳見第四節）。

### 3.4 預期產出

1. 至少 10 組 adapter 及其訓練紀錄（涵蓋 bf16 LoRA、DoRA、QLoRA）
2. 每組的 `vram_report.json`（VRAM 峰值與可訓練參數比例）
3. **「訓練方法 × VRAM × 準確率」對照總表與效率前緣圖**
4. 完整的評估結果表（模型 × 量化格式 × benchmark × 指標）
5. 合併後的完整權重
6. 三種格式的 GGUF 檔案：F16、Q8_0、Q4_K_M
7. **「量化格式 × 檔案大小 × 準確率」對照表**（部署代價）

---

## 四、我要如何評估（How will you evaluate it）

### 4.0 執行環境與評估後端

**整個專案在 Windows 上執行，並刻意分成兩套環境，各司其職：**

| 環境 | 負責階段 | 說明 |
| --- | --- | --- |
| **Python venv** | 資料前處理、訓練、合併權重、GGUF 轉換與量化 | 需要 PyTorch 的 CUDA 反向傳播 |
| **LM Studio（CUDA runtime）** | **評估**：載入 GGUF、產生解答 | 指定的評估後端，於 GPU 上做推論 |

**評估流程：** 訓練完成後，將模型合併、轉為 GGUF 並量化成
F16 / Q8_0 / Q4_K_M，再全部匯入 LM Studio，
透過其 Python SDK（`lmstudio`）搭配 `math-verify` 自動評分。
所有推論皆以 CUDA runtime 執行，並驗證 GPU offload 為全部層數。

**一個必須誠實揭露的方法論限制**：
**LM Studio 只能載入 GGUF**，因此所有評估數字本質上都是**量化後**的表現，
而非訓練出來的 bf16 原版。本專案的處理方式是：

- 以 **F16 GGUF** 作為「最接近訓練權重」的評估版本
- 同時評估 **Q8_0 與 Q4_K_M**，把「微調的效果」與「量化的代價」**分離**
- 在報告中明確標示評估管線為 GGUF

這項限制反而成為優點：它讓量化代價的測量**免費附帶在評估流程中**，
而量化正是本課程的核心主題之一。

### 4.1 評估基準

| Benchmark | 題數 | 難度 | 用途 |
| --- | --- | --- | --- |
| GSM8K（test） | 1,319 | 小學 | 主要指標，靈敏度高 |
| MATH-500 | 500 | 高中競賽 | 標準子集，學術可比性 |
| AIME 2024/2025 | 30 | 競賽級 | 天花板探測 |

### 4.2 三個層次的指標

單看正確率會誤判，因此每個模型都測量三層指標：

**（1）正確率**：最終答案是否正確。使用 `math-verify` 做**數學等價性**判定
（而非字串比對），因此 `1/2` 與 `0.5` 會被視為相同答案。

**（2）格式合規率**：模型是否遵守目標輸出格式。包含
產生 `<think>` 的比例、**閉合** `</think>` 的比例、
產生 `\boxed{}` 的比例、答案抽取成功率，以及**截斷率**。

> 這一層是區分「真的變強」與「只是學會格式」的關鍵。
> 若正確率不變但格式合規率大幅上升，結論應該是後者。

**（3）效率指標**：平均生成 token 數、平均生成時間。

### 4.3 公平比較的設計

**這是最關鍵的方法論考量。** `Qwen3.5-Base` 是預訓練模型，
沒有被教過遵循對話格式。若直接比較，微調模型會因為「終於會輸出格式了」
而得到誇大的進步幅度。

因此基礎模型將建立三條基準線：

- **B0：chat template + thinking 開啟**（`enable_thinking=True`），誠實呈現原始狀態。
- **B1：chat template + thinking 關閉**（`enable_thinking=False`），
  隔離「思考」本身的貢獻。
- **B2：few-shot / completion 風格**，提供 4–8 個訓練格式的完整示範，
  **這才是最公平的對照組**。

**主要結論以「微調後模型」對比「三個基準中最好的那一個」為準。**
若微調模型只贏過最差的基準，那個進步就沒有說服力。
（註：Qwen 未公布 Qwen3.5-2B/4B-**Base** 的數學成績，
因此沒有官方基準線可引用；模型卡上的數字都屬於後訓練版本。）

### 4.4 資料汙染的控制

數學 SFT 資料集常由 MATH 與 GSM8K 建構而成，因此訓練題可能與測試題重疊，
造成「背題」的假進步。處理方式：

1. 以題目正規化後的字串比對，移除訓練集中與測試集重疊的樣本。
2. 在報告中明確揭露重疊比例與去汙染後的結果。
3. 強調**最強的證據是「同題同提示」的比較**——因為汙染對兩者影響相同。

### 4.5 統計嚴謹性

每個準確率數字都會標註樣本數與信賴區間。標準誤為 `sqrt(p(1-p)/n)`：
n=500 時約 ±4.4%（95% CI），n=30（AIME）時高達 ±18%。
因此 ±4% 以內的差異不會被隨意宣稱為真實進步。

更重要的是，由於 base 與微調模型是在**完全相同的題目**上評估，
本專案將使用**配對檢定**（McNemar / 對每題對錯做 bootstrap）
來檢定差異——配對設計遠比比較兩個獨立的信賴區間靈敏。
評估使用 greedy 解碼以確保可重現。

此外，由於 Qwen 官方建議數學難題給予高達 81,920 token 的思考預算，
而本機 16 GB 顯卡無法負擔，本專案會**固定一個 token 預算**
（2B 用 4096、4B 用 8192），對 base 與微調模型使用完全相同設定，
並**同時報告截斷率**，避免「base 因被截斷而低估」造成的假進步。

### 4.6 量化後的評估

將 F16 / Q8_0 / Q4_K_M 三個 GGUF 分別匯入 LM Studio，
以**完全相同的題目、提示與推論參數**重新評估，測量量化造成的準確率損失。

比較時必須注意「同一列才能比」的原則：

- **微調是否有效** → 比較**同一種量化格式**下的 base vs 微調（例如 F16 vs F16）
- **量化損失多少** → 比較**同一個模型**的不同量化（例如 F16 vs Q8_0 vs Q4_K_M）

由於兩者是**同一批題目上的配對資料**，將使用 McNemar 檢定
（而非比較兩條獨立的信賴區間），以取得統計上可信的結論。

---

## 五、可行性（Is this feasible）

### 5.1 硬體可行性

| 資源 | 規格 | 評估 |
| --- | --- | --- |
| GPU | 16 GB VRAM、CUDA | **足夠**。全參數微調不可行，但參數高效微調完全可行，且**不需要量化**：2B 的 bf16 LoRA 實測約 **4.6 GB**、4B 約 **9.5 GB**，兩者都在 16 GB 內。評估階段則由 LM Studio 以 CUDA runtime 執行，GGUF 最大者（4B F16）約 8.4 GB，同樣放得下。 |
| RAM | 64 GB | **充足**。可用來分頁優化器狀態或作為 GGUF 轉換的緩衝。 |
| CPU | 8 核心 | **夠用**。主要用於資料前處理、合併權重與 GGUF 轉換。 |
| 儲存 | 需 150 GB 以上 | 需確認可用空間；模型、資料集與多份 GGUF（F16/Q8/Q4）為主要佔用者。 |
| 作業系統 | Windows x64 | **全程支援**。訓練走 Python venv；評估走 LM Studio Windows 版（官方支援 x64）。 |

**已知限制（以及本專案如何處理它）**：

16 GB VRAM **無法**執行全參數微調（4B 約需 56 GB、2B 約需 27 GB，
這是數量級上的差距，不是調參數能解決的）。
因此本專案**不把「FFT vs LoRA」當作主要實驗軸**，而是改用更有意義的替代問題：

> **在固定的 16 GB 硬體預算下，哪一種參數高效微調方法
> 能達到最好的能力／資源比？**

這正是 **E 軸（微調方法與訓練精度）** 要回答的問題，
也直接對應課程的「Efficient Fine-Tuning」主題。
**硬體限制因此從「缺陷」變成了「研究設計的核心約束」。**

### 5.2 軟體可行性

**訓練端（Python venv，Windows 原生）：**

- **支援明確**：Unsloth 官方文件已明確支援 Qwen3.5 全系列
  （0.8B/2B/4B/9B/27B 等）的 16-bit LoRA 與全參數微調，
  並提供 2B/4B 的免費 Colab 範例。**其官方 4B 範例本身就是 16-bit LoRA。**
- **必須注意**：Qwen3.5 需要 `transformers v5` 以上版本。
- **必須注意（重要）**：**Qwen3.5 的 Gated DeltaNet 層在 fp16 下會產生 NaN 梯度**，
  官方因此強制此架構使用 `float32`/`bf16`。
  本專案固定使用 `bf16=True`，**絕不使用 fp16**。
- **已知問題與本專案的處理方式**：官方**不建議**對 Qwen3.5 做 4-bit（QLoRA）訓練，
  而且我們查證後發現**標準 QLoRA 在此架構上目前會直接崩潰**——原因是
  Unsloth 內部的「不量化清單」以 `mamba` 比對，而 Qwen3.5 的模組名稱是
  `linear_attn`，兩者對不上，導致 Gated DeltaNet 的投影層被 4-bit 量化後
  未正確解量化（社群有兩個仍開啟的 issue，且有隔離測試顯示
  Qwen2.5 / gemma-2 等純注意力模型可正常訓練，只有混合架構失敗）。

  本專案的處理方式是**把它當成一個可測量的研究問題，而非直接迴避**：

  1. **主力一律使用 bf16 LoRA**：因為實測顯示 2B 只需 4.6 GB、4B 只需 9.5 GB，
     **在 16 GB 內根本不需要量化**。這是最穩妥、品質最有保障的路線。
  2. **把 QLoRA 保留為正式對照組**：包含一組**刻意重現崩潰**的實驗，
     用來記錄完整的錯誤訊息。這讓我們能引用**自己重現的證據**，
     而不是只轉述別人的 issue。
  3. **提供可運作的修正版 QLoRA**：透過自訂 `BitsAndBytesConfig`
     搭配 `llm_int8_skip_modules` 把線性注意力層排除在量化之外。
     但修正後只能省約 1.4 GB（因為 PEFT 會把被跳過的層升到 fp32），
     效益不彰——**這個「修正後也不划算」的量化結論本身就是研究產出**。
  4. **備妥替代的 4-bit 路線**：HQQ（PEFT 官方支援 LoRA 訓練，
     且支援逐層位元數設定），列為探索性實驗。

  這樣的設計讓「官方不建議」與「實測會崩潰」這兩件事
  本身成為專案的實驗內容，而不是專案的阻礙。

**評估端（LM Studio，Windows 原生）：**

- **LM Studio 官方支援 Windows x64**，並提供 **CUDA 版 llama.cpp runtime**，
  符合本專案「runtime 必須是 CUDA」的要求。
- 評估透過官方 **Python SDK（`pip install lmstudio`）** 驅動，
  可直接指定 `gpu.ratio = 1.0` 確保全部層數在 GPU 上執行。
- **GGUF 為其唯一支援的模型格式**，因此評估本質上測量的是量化後模型
  （已於第 4.0 節說明並納入設計）。
- **不需額外安裝推論框架**（無須 vLLM／WSL2／llama.cpp 編譯），
  大幅降低 Windows 上的環境風險。

### 5.3 主要風險與因應

| 風險 | 可能性 | 影響 | 因應方案 |
| --- | --- | --- | --- |
| **GGUF 轉換的 MTP 層問題** | 中 | 低 | 這是最大的技術風險，但**根因已釐清且有明確解法**。Qwen3.5 的檢查點含一個 MTP（多 token 預測）區塊，因此 2B 實際有 25 塊、4B 有 33 塊，轉換時若一併打包會導致載入失敗（社群多個 GitHub issue 即源於此）。**解法是在轉換時加上 `--no-mtp`**，並使用 2026-09-07 之後的 llama.cpp。此參數對數學微調無任何損失（MTP 只用於加速推論）。備援方案：改用 Unsloth 內建的 GGUF 匯出。 |
| **QLoRA 在此架構上崩潰** | **高（已確認）** | 低 | 這**不是風險而是已確認的事實**：Unsloth 的不量化清單以 `mamba` 比對，對不上 Qwen3.5 的 `linear_attn`，導致 Gated DeltaNet 被 4-bit 量化後未正確解量化（兩個仍開啟的社群 issue，且有隔離測試佐證）。因應：**主力改用 bf16 LoRA**（本來就放得下）；QLoRA 則保留為對照組，並用一組實驗**刻意重現崩潰**以取得第一手證據。 |
| **4B 模型 OOM** | 低 | 低 | bf16 LoRA 實測約 9.5 GB，在 16 GB 內有約 6 GB 餘裕，**預期不會 OOM**。若發生，因應順序：`paged_adamw_8bit` → 降低序列長度 → 降低 rank → 只微調注意力層。**不建議**用 QLoRA 救 OOM（它會崩潰）。 |
| bitsandbytes 環境設定失敗 | 中 | 低 | 只影響進階的 QLoRA/HQQ 實驗，**不影響主力流程**（bf16 LoRA 不需要 bitsandbytes）。 |
| 資料集下載過慢或佔空間過大 | 中 | 低 | 使用 Hugging Face 鏡像端點；先用下載量較小的 `default` config（約 2.1 GB）。 |
| 訓練時間超出預期 | 中 | 中 | 實驗組數較多（約 20 組），優先完成 E 軸（方法對照）與 A 軸（規模對照），共 6 組；B、C、D、F 軸依剩餘時間追加。 |
| 部署量化後品質崩壞 | 中 | 低 | 這本身就是有價值的發現。因應：使用 imatrix、把 `attn_*` 層留在較高精度（`--tensor-type`）、或退回 Q8_0。 |
| 評估工具在 Windows 上靜默失效 | 中 | 中 | 已知 `math-verify` 在 Windows 會靜默判定所有答案為錯誤。因應：明確傳入 `parsing_timeout=None` 與 `timeout_seconds=None` 關閉內建逾時，並先用已知答案的樣本驗證判分邏輯。 |

### 5.4 時程（四週）

| 週次 | 工作內容 |
| --- | --- |
| **第 1 週** | 建立 venv 與 CUDA 環境；安裝 LM Studio 並確認 CUDA runtime；下載模型；資料前處理；bf16 路徑的煙霧測試 |
| **第 2 週** | 完成 2B 與 4B 的 bf16 LoRA 訓練；合併權重、轉 GGUF、匯入 LM Studio；建立評估流程並跑出第一組數字 |
| **第 3 週** | 完成 F16 / Q8_0 / Q4_K_M 三種量化的完整評估與配對檢定；完成 base 基準線；擴充實驗（DoRA、rank、資料量、層選擇）；重現並記錄 QLoRA 崩潰 |
| **第 4 週** | 整理「訓練方法 × VRAM × 準確率」與「量化格式 × 大小 × 準確率」兩張對照表；繪製效率前緣圖；撰寫報告 |

### 5.5 結論

本專案在硬體、軟體與資料三方面皆具備可行性：

- **模型**：公開、免費、Apache-2.0、官方工具鏈明確支援微調。
- **資料**：公開、已含思考痕跡、不需自行建構。
- **硬體**：16 GB VRAM 雖無法執行全參數微調，但足以執行 bf16 LoRA
  （2B 實測約 4.6 GB、4B 約 9.5 GB），**兩者都在 16 GB 內且留有餘裕**，
  因此本專案**不需要依賴量化就能完成全部核心實驗**。
  官方自己的 Qwen3.5-4B 範例就是在 14.56 GB 的 T4 上跑 16-bit LoRA，
  這是已被驗證的可行路線。
- **軟體**：訓練端使用 Windows 原生的 Python venv；
  評估端使用 **Windows 原生的 LM Studio + CUDA runtime**，
  **不需要 WSL2、不需要編譯 llama.cpp、不需要 vLLM**，
  環境風險大幅低於其他可能方案。
- **方法**：所有指標（VRAM 峰值、可訓練參數比例、準確率、
  格式合規率、截斷率、量化後檔案大小）皆可自動化計算，
  具客觀性與可重現性。

**兩個主要風險都有明確因應：**

1. **GGUF 工具鏈的成熟度** —— 根因已被完整診斷（MTP 區塊的打包問題），
   社群已有可重現的解法（`--no-mtp` 搭配新版 llama.cpp），
   另有 Unsloth 內建匯出作為備援。
2. **QLoRA 在此架構上不可用** —— 這原本是最大的不確定性，
   但查證後已確認**標準 QLoRA 會直接崩潰**（成因與 issue 皆已定位）。
   由於主力方法改為 bf16 LoRA，**此問題已不影響專案的核心實驗**；
   它反而成為一組有引用價值的對照實驗。

因此，本專案不只可行，其**硬體限制本身還構成了研究設計的核心**
——「在 16 GB 的預算下，哪一種微調方法能換到最好的數學推理能力？」
這正是課程「Efficient Fine-Tuning」主題的直接回應。
